No additional pruning happens in epsilon.
\sembox{
    $\semaut{\epsilon}
    =\automaton$
    
    Where
    
    $Q=\{s, f\}$

    $C=\{x\}$
    
    $\Delta=\{(s,x=0,\emptyset,\varepsilon,f)\}$
    
    $F=\{f\}$
}
